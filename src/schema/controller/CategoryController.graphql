type Query {
    categories: [Category]
    category(id: Int): Category
}

type Mutation {
    categorySave(id: Int, name: String): Category
    addCategoryChild(parentId: Int, childId: Int): Category
}
